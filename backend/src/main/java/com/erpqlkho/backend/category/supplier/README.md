# Module: Supplier (Nhà cung cấp)

Chức năng: CAT-03. Field gợi ý: code, name, contactPerson, phone, email, address.

Cấu trúc cần code (theo pattern mẫu ở `auth/` + `user/`): `entity/`, `repository/`, `service/`, `controller/`, `dto/`.
