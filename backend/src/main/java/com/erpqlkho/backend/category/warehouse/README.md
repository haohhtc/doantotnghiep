# Module: Warehouse (Kho)

Chức năng: CAT-04. Field gợi ý: code, name, address, managerId (ManyToOne → User, optional).

Cấu trúc cần code (theo pattern mẫu ở `auth/` + `user/`): `entity/`, `repository/`, `service/`, `controller/`, `dto/`.
