# Module: ProductCategory (Danh mục sản phẩm)

Chức năng: CAT-02. Hỗ trợ phân cấp cha/con (`parent_id` tự tham chiếu).

Cấu trúc cần code (theo pattern mẫu ở `auth/` + `user/`): `entity/`, `repository/`, `service/`, `controller/`, `dto/`.
