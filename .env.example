# --- MySQL OLTP (dùng bởi backend Java) ---
OLTP_DB_HOST=localhost
OLTP_DB_PORT=3306
OLTP_DB_NAME=erp_qlkho_oltp
OLTP_DB_USER=erp_user
OLTP_DB_PASSWORD=erp_password
MYSQL_ROOT_PASSWORD=root_password

# --- MySQL Data Warehouse (dùng bởi etl, ai-services) ---
DW_DB_HOST=localhost
DW_DB_PORT=3307
DW_DB_NAME=erp_qlkho_dw
DW_DB_USER=erp_user
DW_DB_PASSWORD=erp_password

# --- Backend ---
JWT_SECRET=change-me-to-a-long-random-secret
JWT_EXPIRATION_MS=86400000
SERVER_PORT=8080

# --- Frontend ---
VITE_API_BASE_URL=http://localhost:8080/api
VITE_AI_FORECASTING_URL=http://localhost:8001
VITE_AI_CHATBOT_URL=http://localhost:8004

# --- AI Services ---
FORECASTING_PORT=8001
STOCK_RISK_PORT=8002
ANOMALY_DETECTION_PORT=8003
CHATBOT_PORT=8004
# LLM provider cho chatbot (nếu dùng Claude/OpenAI API)
LLM_API_KEY=
